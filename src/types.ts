export enum Color {
    Primary = 'primary',
    Secondary = 'secondary',
    Default = 'default'
}

export enum LabelPlacement {
    Start = 'start',
    End = 'end',
    Top = 'top',
    Bottom = 'bottom'
}